# 2025_Nov_MDS_DSCI522
MDS DSCI552 public repository

This was created during the DSCI 552 class on Nov 18

- `git branch <name>` create a new branch
- `git switch <name>` switch to the created branch
    - `git switch -c <name>` create and switch to new branch

- `git branch -d <name>` will delete the branch <name>